import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";

export function registerPrompts(server: McpServer) {
  // TODO: Register your prompts here
  // Example:
  // server.registerPrompt("my-prompt", { ... }, async () => ({ messages: [...] }));
  void server;
}
